/*public class Classe {

    public double metodo(List<Lista> num, double valor) {
        //verifica a quatidade de numeros
        //List<Lista> list = new ArrayList<>();

        /*for (Lista n : num) {
            if (n < 1 || n > 60) {
                return 0.0;
            }
            if (list.contains(n)) {
                return 0.0;
            }
            list.add(n);
        }

        // metodo
        if (list.size() >= 6 && list.size() <= 15) {
            //sorteia os numeros
            List<Integer> list2 = new ArrayList<>();
            int s;
            while (list2.size() < 6) {
                s = new Random().nextInt(59) + 1;
                if (!list2.contains(s)) {
                    list2.add(s);
                }
            }
            //contagem de acertos
            int tot = 0;
            for (Lista i : num) {
                if (list2.contains(i)) {
                    tot++;
                }
            }

            //metodo
            //calcula a quantidade de acertos e o valor do premio
            public String sorteio () {
                if (tot == 6) {
                    return valor;
                } else if (tot == 5) {
                    return valor * 0.2;
                } else if (tot == 4) {
                    return valor * 0.05;
                }
            }
            return valor;

        }

    }
}*/
//alterar os numeros magicos por variaveis
//alterar os nomes das variavies
//criar metodos
//alterar o nome da classe
//separar os metodos da classe principal
//criar classes diferentes
//validar os atritubos fazendo o tratamento com exceção
//construir os casos de testes

